#include <iostream>
#include <vector>
#include <string>
#include <algorithm>
#include <sstream>

using namespace std;

vector<int> solution(vector<string> genres, vector<int> plays);
{
	vector<string> genres_input;
	return answer
}